import {MockDataMethodsType} from "../types";


export const methodsTypeMap: Record<string, MockDataMethodsType> = {
  '@string': 'string',
  '@number': 'number',
  '@boolean': 'boolean',
  '@date': 'date',
  '@array': 'array',
  '@object': 'object',
}
